# 两台 Mac 的 Codex 协作规则

本仓库以 GitHub 的 `main` 分支作为两台 Mac 之间唯一的稳定代码源。Mac mini 负责构建，MacBook Air 负责签名并安装到真机。

## 开始任务

1. 先运行 `git status --short`。
2. 如果工作区干净，运行 `./ci/safe-sync.sh main`，再从最新 `main` 创建 `codex/<简短任务名>` 分支。
3. 如果工作区有改动，视为当前未完成任务；不要自动拉取、切换分支或覆盖文件，先理解并继续现有工作。
4. 一个任务使用一个分支。两台 Mac 同时开发时，应修改不同页面或模块，避免同时编辑项目配置文件。

## 用户说“完成并同步”或“提交并同步”

1. 检查改动范围并运行与改动相关的测试。
2. 提交当前任务并推送当前分支。
3. 不要在另一台机器的工作区不干净时强制同步；另一台 Codex 会在下次开始任务时安全同步 `main`。

## 用户说“安装到真机”

默认含义是把当前已完成任务整合为两台 Mac 的稳定版本：

1. 检查并提交当前任务的全部相关改动，运行相关测试并推送任务分支。
2. 获取远端最新状态，将任务分支合并到最新 `main`。若存在无法可靠判断的冲突，停止并请用户选择，禁止强制覆盖。
3. 推送 `main`。`Build on Mac mini and install on iPhone` 工作流会自动在 Mac mini 构建，并在 MacBook Air 签名和安装。
4. 等待 GitHub Actions 完成；只有构建和安装任务均成功，才报告安装完成。
5. 当前电脑工作区干净时，运行 `./ci/safe-sync.sh main`，使它停留在最新稳定代码。

如果用户明确要求只临时测试一个尚未合并的分支，则不要改动 `main`；使用 GitHub Actions 的手动入口并将 `git_ref` 设置为该分支或提交 SHA。

## 安全边界

- 禁止对有未提交改动的工作区自动执行拉取、切换、重置或清理。
- 禁止使用强制推送、`git reset --hard` 或覆盖另一台电脑的提交。
- `main` 推送代表要安装到真机；普通任务分支推送不会自动安装。
- 合并前必须先获取远端最新状态，优先保留清晰的任务提交历史。
