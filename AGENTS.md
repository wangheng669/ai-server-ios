# 两台 Mac 的 Codex 协作规则

本仓库以 GitHub 的 `main` 分支作为两台 Mac 之间唯一的稳定代码源。Mac mini 负责构建，MacBook Air 负责签名并安装到真机。

## 开始任务

1. 先运行 `git status --short`。
2. 如果工作区干净，运行 `./ci/safe-sync.sh main`，直接在最新 `main` 上继续开发。
3. 如果工作区有改动，视为当前未完成任务；不要自动拉取、切换分支或覆盖文件，先理解并继续现有工作。
4. 两台 Mac 同时开发时，尽量修改不同页面或模块，避免同时编辑项目配置文件。
5. 只有大型改动、实验性改动，或明确预计会与另一台 Mac 修改相同文件时，才创建 `codex/<简短任务名>` 分支。

## 用户说“完成并同步”或“提交并同步”

1. 检查改动范围并运行与改动相关的测试，然后提交当前任务。
2. 推送前获取远端最新状态。如果另一台 Mac 已推送新提交，将其安全合并；若发生无法可靠判断的冲突，停止并请用户选择。
3. 推送 `main`。如果当前任务使用了临时分支，则先将其安全合并到最新 `main` 后再推送。
4. 不要在另一台机器的工作区不干净时强制同步；另一台 Codex 会在下次开始任务时安全同步 `main`。

## 用户说“安装到真机”

默认含义是把当前 `main` 作为两台 Mac 的最新稳定版本安装：

1. 如果存在未提交改动，按“完成并同步”的流程完成提交和推送。
2. 如果 `main` 已经同步但没有新的推送，手动触发 `Build on Mac mini and install on iPhone`，并将 `git_ref` 设为 `main`。
3. 等待 GitHub Actions 完成；只有 Mac mini 构建和 MacBook Air 安装均成功，才报告安装完成。
4. 当前电脑工作区干净时，运行 `./ci/safe-sync.sh main`，使它停留在最新稳定代码。

如果用户明确要求只临时测试一个尚未合并的分支，则不要改动 `main`；使用 GitHub Actions 的手动入口并将 `git_ref` 设置为该分支或提交 SHA。

## 安全边界

- 禁止对有未提交改动的工作区自动执行拉取、切换、重置或清理。
- 禁止使用强制推送、`git reset --hard` 或覆盖另一台电脑的提交。
- `main` 推送会自动安装到真机；已经同步且没有新提交时，可通过手动入口重新安装。
- 合并前必须先获取远端最新状态，优先保留清晰的任务提交历史。
